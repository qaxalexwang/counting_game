# 瞬间计数挑战 (Instant Count Challenge)

一个高强度的视觉记忆与瞬时反应游戏。玩家需要在极短的时间内准确记下屏幕上飞过的小鸟数量。

## 🎮 游戏核心玩法

1.  **准备阶段**：3秒倒计时，让你集中注意力。
2.  **展示阶段**：成群的小鸟以不同速度、不同轨迹穿过屏幕。随着关卡提升，小鸟数量增加，速度变快，且会出现严重的视觉遮挡。
3.  **答题阶段**：你有 **3秒钟** 的限时，通过数字面板或键盘输入你看到的小鸟数量。
4.  **反馈阶段**：显示正确答案并根据你的表现计算得分。

## 🏆 得分规则

*   **精准计数**：输入完全正确，获得该关卡基础分 + 连击奖励（每连击一次 +5分）。
*   **漏算补偿**：如果输入的数量**小于**实际数量，将按比例获得部分分数（例如：实际10只，猜对8只，获得80%分数），但连击会重置。
*   **多算惩罚**：如果输入的数量**大于**实际数量，视为观察错误，本轮不得分且连击重置。
*   **限时压力**：必须在 3 秒内提交，超时自动提交当前输入或视为 0 分。
*   **关卡权重**：关卡越高基础分越高，第 5 关（专家级）基础分为 100 分。

## 🛠️ 技术特性

*   **React 19 & Tailwind CSS**：现代化的前端 UI 框架与样式工具。
*   **Canvas 动画**：使用 HTML5 Canvas 和 二次贝塞尔曲线 (Bezier Curve) 实现丝滑的小鸟飞行轨迹。
*   **Web Audio API**：实时合成背景音乐，根据游戏状态（如展示阶段）动态调整频率和音量。
*   **响应式设计**：适配 PC 与移动端显示。

## 🚀 如何部署与运行

本项目采用 **Native ESM (原生 ES 模块)** 架构，使用了 `importmap` 直接从 CDN 加载 React。这意味着在大多数情况下，你**不需要**复杂的 `npm install` 或 `npm run build` 步骤即可运行。

### 1. 本地运行

由于浏览器出于安全考虑限制了 `file://` 协议下的模块加载，你需要使用一个简单的静态服务器：

*   **VS Code 用户**：安装 `Live Server` 插件，右键点击 `index.html` 选择 "Open with Live Server"。
*   **命令行用户**（需要 Node.js）：
    ```bash
    # 在项目根目录下运行
    npx serve .
    ```
    然后访问终端显示的本地地址（通常是 `http://localhost:3000`）。

### 2. 部署到静态托管平台

由于项目是纯静态的，你可以将其轻松部署到以下平台：

#### 方案 A：GitHub Pages
1. 将项目代码上传到 GitHub 仓库。
2. 进入仓库设置 (Settings) -> Pages。
3. 在 Build and deployment 部分，选择 `Deploy from a branch`。
4. 选择 `main` 分支并点击 Save。
5. 几分钟后，你的游戏将通过 `https://<your-username>.github.io/<repo-name>/` 访问。

#### 方案 B：Vercel / Netlify
1. 登录 Vercel 或 Netlify。
2. 关联你的 GitHub 仓库或直接拖拽项目文件夹到上传区。
3. **关键设置**：由于本项目直接运行，不需要 "Build Command"（留空）和 "Output Directory"（设为 `.`）。
4. 点击 Deploy 即可。

## 📁 项目结构

*   `index.html`: 入口文件，配置了 `importmap`。
*   `index.tsx`: React 挂载点。
*   `App.tsx`: 核心游戏逻辑与状态管理。
*   `constants.ts`: 关卡配置与视觉参数。
*   `types.ts`: TypeScript 类型定义。
*   `components/`:
    *   `BirdCanvas.tsx`: 负责高性能图形渲染。
    *   `Numpad.tsx`: 数字输入面板。
    *   `MusicEngine.tsx`: Web Audio 音乐引擎。

---
*祝你在瞬间计数挑战中取得高分！*
